<Window x:Class="SistemaDeInventarioASOEM.windows.VentanaEliminarProducto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Eliminar Producto" Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </Window.Resources>

    <Border Padding="10" Background="#FFF5F5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="⚠️ ATENCIÓN" 
                           FontSize="24" FontWeight="Bold" Foreground="#C0392B" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Eliminación manual de producto" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="20,0">
                <TextBlock Text="Escriba el ID o el Nombre exacto:" FontWeight="Bold"/>

                <TextBox x:Name="TxtInput"
                         Text="{Binding TerminoInput, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                         Height="40" 
                         FontSize="16" 
                         VerticalContentAlignment="Center" 
                         Background="White"
                         BorderBrush="Black"
                         BorderThickness="1"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,10">
                <Button Content="ELIMINAR" 
                        Command="{Binding EjecutarEliminacionCommand}"
                        Width="120" Height="40" Margin="0,0,10,0"
                        Background="#C0392B" Foreground="White" FontWeight="Bold"/>

                <Button Content="Cancelar" 
                        Command="{Binding CancelarCommand}"
                        IsCancel="True"
                        Width="100" Height="40"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>